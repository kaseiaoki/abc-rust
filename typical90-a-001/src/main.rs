// -*- coding:utf-8-unix -*-

use proconio::input;

// ABC typical90 a 001

fn main() {
    input! {
        N: usize,
        L: usize,
        K: usize,
        A: [usize; N],
    }
    // print!("{} {} {} {:?}", N, L, K, A);
}
